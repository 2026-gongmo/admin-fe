# ONDA Admin Web environment example
# 현재 프론트는 Mock 데이터 기반입니다.
# 실제 Spring Boot API 연결 단계에서만 아래 값을 사용하세요.

# 예: http://127.0.0.1:8080
VITE_API_BASE_URL=

# mock | http
# 현재 기본값은 mock으로 유지하는 것을 권장합니다.
VITE_API_MODE=mock
